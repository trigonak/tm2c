PLATFORM_NUMA=0

PLATFORM_CFLAGS = -O3
PLATFORM_LDFLAGS = 
PLATFORM_LIBS = -lssmp -lrt

## fixed defines
PLATFORM_DEFINES +=    	-DPLATFORM_DEFAULT \
		       	-DSSMP

ifeq ($(PLATFORM_NUMA),1)
PLATFORM_LIBS += -lnuma
PLATFORM_DEFINES += -DPLATFORM_NUMA
endif

ARCHIVE_SRCS_PURE += tm2c_malloc.c
ARCHIVE_SRCS_PURE += sys_default.c

ALL_BMARKS = bank mbll ht
